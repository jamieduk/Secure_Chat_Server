#!/bin/bash
# ./run_an_watch.sh "program / script"
#watch df -h $1
#

# You can add this script into /etc/rc.local file (before exit line), e.g.

# /home/myuser/go.py &

#Where & at the end will run the script in the background.
#
